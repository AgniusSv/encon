{% extends 'base.html.twig' %}

{% block body %}
    <h1>Rfid</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rfid.id }}</td>
            </tr>
            <tr>
                <th>Rfidid</th>
                <td>{{ rfid.rfidId }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('rfid_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('rfid_edit', { 'id': rfid.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
